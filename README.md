# 🛡️ LAZARUS Backup Manager

![Bash](https://img.shields.io/badge/Language-Bash-4EAA25?style=flat-square)
![License](https://img.shields.io/github/license/UnderGut/LAZARUS-Backup-Manager?style=flat-square)
![Version](https://img.shields.io/badge/version-v4.3.0-blue?style=flat-square)

**Lazarus** — продвинутая автоматизированная система для управления жизненным циклом резервных копий **Remnawave Telegram Shop Bot**.

Скрипт создан, чтобы закрыть вопрос безопасности данных: он сам находит вашего бота в Docker, делает бэкапы, отправляет их в Telegram и умеет “воскрешать” проект из пепла одной командой.

---

## ✨ Возможности

- **🔍 Smart Scan Engine** — автоматически находит папку с ботом, определяет контейнеры и PostgreSQL. Не нужно вручную разбирать `docker-compose`.
- **📦 Полный бэкап (Full)** — единый архив, содержащий:
  - дамп базы данных (PostgreSQL),
  - файлы проекта (uploads/переводы/конфиги и т.д.),
  - информацию о текущей версии бота.
- **✈️ Telegram Upload** — отправляет архив вам в личку или в админ-чат.
  - **Smart Limit:** если файл больше лимита Telegram (≈50 MB), скрипт уведомит и оставит архив локально на сервере.
- **🔄 Автоматизация (Cron)** — встроенный менеджер задач: настройка частоты (час/день/неделя) прямо из меню, без ручного редактирования crontab.
- **🧹 Ротация (Auto-Cleanup)** — удаление старых бэкапов (по умолчанию старше 7 дней), чтобы не забивать диск.
- **❤️‍🩹 Restore** — удобное восстановление из меню:
  - **Full Restore** — файлы + БД,
  - **DB Only** — только база,
  - **Files Only** — только файлы.
- **🛡️ Безопасность**
  - защита от случайного удаления (код подтверждения `666`),
  - проверка целостности конфигурации при каждом запуске.

---

## ✅ Требования

- Linux (Debian/Ubuntu/etc.)
- `bash`, `tar`, `gzip`
- `docker` и `docker compose`
- `wget` или `curl`
- Доступ к Docker-сокету (обычно root)

> Дамп БД создаётся через PostgreSQL-инструменты внутри окружения контейнеров (скрипт сам определяет, как корректно снять backup).

---

## 🚀 Установка

Одна команда — скачает скрипт, сделает исполняемым и установит в `/usr/local/bin/lazarus`:

### Вариант через `wget`
```bash
wget -O /usr/local/bin/lazarus \
  https://raw.githubusercontent.com/UnderGut/LAZARUS-Backup-Manager/main/lazarus-backup \
  && chmod +x /usr/local/bin/lazarus
```

### Вариант через `curl`
```bash
curl -fsSL \
  https://raw.githubusercontent.com/UnderGut/LAZARUS-Backup-Manager/main/lazarus-backup \
  -o /usr/local/bin/lazarus \
  && chmod +x /usr/local/bin/lazarus
```

---

## 🎮 Использование

Запуск:
```bash
lazarus
```

---

## 🧩 Первичная настройка

При первом запуске Lazarus просканирует систему и попытается найти установленные копии **Remnawave Bot**. Затем попросит данные для отправки отчётов/архивов в Telegram:

- **Bot Token** (от `@BotFather`) — токен бота, который будет присылать файлы.
- **Chat ID** (получить у `@userinfobot`) — ваш ID или ID чата, куда слать бэкапы.
- **Thread ID** *(опционально)* — если вы используете супергруппу с темами (topics).

Конфигурация сохраняется в:
```
/opt/lazarus-backup/config.env
```

> ⚠️ **Важно:** этот файл содержит токен Telegram-бота. Не публикуйте его и не передавайте третьим лицам.

---

## 🖥️ Меню управления (пример)

```text
LAZARUS Backup Manager v4.3.0
Версия бота: 3.19.5

Статусы авто-бекапа:
 • Full:  Ежедневно 04:00
 • DB:    Ежечасно
 • Files: Выкл

 --- ДЕЙСТВИЯ ---
 1. Ручной бекап (Full / DB / Files)
 2. Настроить авто-бекап
 3. Восстановить из бекапа
 4. Настройки (Пути / Токены / Ротация)
 5. Удалить старые бэкапы (>7 дней)

 666. Удалить скрипт (Uninstall)
 0. Выход
```

---

## 🔁 Авто-бэкапы (Cron)

Lazarus умеет сам:
- создавать/обновлять cron-задачи,
- включать разные типы бэкапов (Full/DB/Files),
- выключать авто-бэкап без ручной правки crontab.

---

## 🛠 Обновление

Чтобы обновить Lazarus до последней версии — просто выполните команду установки заново.  
Ваши настройки и архивы **не удаляются**.

---

## 🧯 Восстановление (Restore)

В меню восстановления доступны режимы:
- **Full Restore** — возвращает файлы + базу,
- **DB Only** — откатывает только PostgreSQL,
- **Files Only** — восстанавливает только файлы проекта.

> Рекомендуется делать restore в “окне обслуживания”, когда бот остановлен.

---

## ⚠️ Важно

- Проверяйте свободное место на диске (особенно при Full-бэкапах с медиа).
- Если Telegram не принял файл из-за размера — архив останется локально, а вы получите уведомление.
- Никогда не коммитьте `/opt/lazarus-backup/config.env` в публичные репозитории.

---

## 📄 Лицензия

См. файл `LICENSE` в репозитории.

---

<div align="center">
  Developed by <b>UnderGut</b>
</div>
