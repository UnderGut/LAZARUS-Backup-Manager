<p align="center">
  <img src="main_menu.png" alt="LAZARUS main menu" width="600">
</p>

# 🛡️ LAZARUS Backup Manager

![Bash](https://img.shields.io/badge/Language-Bash-4EAA25?style=flat-square)
![License](https://img.shields.io/github/license/UnderGut/LAZARUS-Backup-Manager?style=flat-square)
![Version](https://img.shields.io/badge/version-v4.4.3-blue?style=flat-square)

**Lazarus** — продвинутая автоматизированная система для управления жизненным циклом резервных копий **Remnawave Telegram Shop Bot**.

Скрипт создан, чтобы закрыть вопрос безопасности данных: он сам находит вашего бота в Docker, делает бэкапы, отправляет их в Telegram и умеет «воскрешать» проект из пепла одной командой.

---

## ✨ Возможности

- **🔍 Smart Scan Engine** — автоматически находит папку с ботом, определяет имена контейнеров и базу данных PostgreSQL. Не нужно вручную разбирать `docker-compose`.
- **📦 Полный бэкап (Full)** — единый архив, содержащий:
  - дамп базы данных (PostgreSQL),
  - файлы проекта (конфиги, переводы и т.д.),
  - информацию о текущей версии бота.
- **🚫 Гибкие исключения** — возможность исключить тяжелые папки (например, `uploads` или `node_modules`) через настройки.
- **✈️ Telegram Upload** — отправляет архив вам в личку или в админ-чат.
  - **Smart Limit:** если файл больше лимита Telegram (≈50 MB), скрипт уведомит об этом и оставит архив локально на сервере.
- **🔄 Автоматизация (Cron)** — встроенный менеджер задач: настройка частоты (час/день/неделя) прямо из меню, без ручного редактирования `crontab`.
- **🧹 Ротация (Auto-Cleanup)** — удаление старых бэкапов (по умолчанию старше 7 дней), чтобы не забивать диск.
- **❤️‍🩹 Restore** — удобное восстановление из меню:
  - **Full Restore** — файлы + БД,
  - **DB Only** — только база,
  - **Files Only** — только файлы.
- **🛡️ Безопасность**
  - защита от случайного удаления (код подтверждения `666`),
  - проверка целостности конфигурации при каждом запуске.

---

## ✅ Требования

- Linux (Debian/Ubuntu/CentOS и др.)
- `bash`, `tar`, `gzip`
- `docker` и `docker compose`
- `wget` или `curl`
- Права `root` (для доступа к Docker-сокету и управления Cron)

> **Примечание:** Дамп БД создаётся через штатные инструменты внутри контейнера Postgres. Скрипту не нужен прямой доступ к портам базы — только доступ к Docker.

---

## 🚀 Установка

Одной командой — скрипт скачается, станет исполняемым и будет готов к работе (устанавливается в `/usr/local/bin/lazarus`).

### Вариант через `wget`
```bash
wget -O /usr/local/bin/lazarus \
  https://raw.githubusercontent.com/UnderGut/LAZARUS-Backup-Manager/main/lazarus-backup \
  && chmod +x /usr/local/bin/lazarus
```

### Вариант через `curl`
```bash
curl -fsSL \
  https://raw.githubusercontent.com/UnderGut/LAZARUS-Backup-Manager/main/lazarus-backup \
  -o /usr/local/bin/lazarus \
  && chmod +x /usr/local/bin/lazarus
```

---

## 🎮 Использование

Для запуска просто введите в терминале:

```bash
lazarus
```

---

## ⚙️ Настройка и конфигурация

При первом запуске Lazarus просканирует систему и попытается найти установленные копии Remnawave Bot. Вам останется только ввести данные для отправки отчётов.

Конфигурация сохраняется по пути:
```
/opt/lazarus-backup/config.env
```

### Описание переменных `config.env`

| Переменная | Описание | Пример |
|---|---|---|
| `BOT_TOKEN` | Токен бота, который будет отправлять бэкапы (от `@BotFather`). | `123456:ABC-DEF...` |
| `CHAT_ID` | Ваш ID или ID чата/канала для получения файлов. | `12345678` или `-100...` |
| `TG_MESSAGE_THREAD_ID` | *(Опционально)* ID темы, если чат — супергруппа с топиками. | `42` |
| `BOT_PATH` | Полный путь к папке с `docker-compose.yml` бота. | `/opt/remnawave` |
| `BOT_CONTAINER_NAME` | Имя Docker-контейнера бота (определяется авто-сканом). | `shopbot` |
| `DB_CONTAINER_NAME` | Имя Docker-контейнера базы данных. | `remnawave-db` |
| `DB_USER` | Пользователь БД (обычно `postgres`). | `postgres` |
| `RETAIN_BACKUPS_DAYS` | Сколько дней хранить бэкапы на диске. | `7` |
| `EXCLUDE_DIRS` | Папки для исключения из бэкапа (через пробел). | `uploads .cache` |

> ⚠️ **Важно:** `config.env` содержит чувствительные данные. Никогда не передавайте его третьим лицам и не загружайте в публичные репозитории.

---

## 🖥️ Меню управления (пример)

```text
LAZARUS Backup Manager v4.4.3
Версия бота: 3.19.5

Статусы авто-бекапа:
 • Full:  Ежедневно 04:00
 • DB:    Ежечасно
 • Files: Выкл

 --- ДЕЙСТВИЯ ---
 1. Ручной бекап (Full / DB / Files)
 2. Настроить авто-бекап
 3. Восстановить из бекапа
 4. Настройки (Пути / Токены / Ротация)
 5. Удалить старые бэкапы (>7 дней)

 666. Удалить скрипт (Uninstall)
 0. Выход
```

---

## 🔁 Авто-бэкапы (Cron)

Lazarus умеет управлять планировщиком задач Linux (Cron) через своё меню.

Вы можете настроить расписание: ежечасно, ежедневно (04:00), еженедельно или задать свой интервал (например, каждые 30 минут).

Логи выполнения автоматических задач пишутся в:
```
/var/log/lazarus_backup.log
```

---

## 🛠 Обновление

Чтобы обновить Lazarus до последней версии, просто выполните команду установки заново.

Скрипт заменит исполняемый файл, но ваши настройки (`config.env`) и папка с бэкапами останутся нетронутыми.

---

## 🧯 Восстановление (Restore)

В меню восстановления доступны режимы:

- **Full Restore** — полная очистка папки бота, распаковка файлов и импорт дампа базы.
- **DB Only** — только сброс и импорт базы данных (файлы не трогаются).
- **Files Only** — перезапись файлов поверх текущих (БД не трогается).

❗️ **Внимание:** процесс восстановления (особенно Full) требует остановки контейнеров. Рекомендуется делать это в период низкой активности пользователей.

---

## 📄 Лицензия

Распространяется под лицензией **MIT License**. См. файл `LICENSE` для подробностей.

<div align="center">Developed by <b>UnderGut</b></div>
